services:
  app:
    build:
      context: .
      dockerfile: dockerfile
    restart: always
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - TURSO_DATABASE_URL=${TURSO_DATABASE_URL}
      - TURSO_AUTH_TOKEN=${TURSO_AUTH_TOKEN}
      - NODE_ENV=production
